import { Router } from "express";
const router = Router();

router.get("/", (req, res) => {
  res.status(200).json({ message: "I'm alive!" });
});

export default router;
("/home/sudhir/Downloads/matrix-algebra-for-engineers.pdf");
